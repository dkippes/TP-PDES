name: aterrizar
services:
  postgres:
    image: postgres:16-alpine
    container_name: aterrizar-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aterrizar_db}
      POSTGRES_USER: ${POSTGRES_USER:-aterrizar}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aterrizar123}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aterrizar} -d ${POSTGRES_DB:-aterrizar_db}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - app-network
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: aterrizar-backend
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-aterrizar_db}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME:-${POSTGRES_USER:-aterrizar}}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD:-${POSTGRES_PASSWORD:-aterrizar123}}
      SPRING_JPA_HIBERNATE_DDL_AUTO: ${SPRING_JPA_HIBERNATE_DDL_AUTO:-update}
      JWT_SECRET: ${JWT_SECRET:-dev-jwt-secret-min-32-chars-long-change-me-in-prod}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-3600000}
      FLYING_SERVICE_URL: ${FLYING_SERVICE_URL:-http://flying-service:8081}
      CORS_ALLOWED_ORIGIN: http://localhost:${FRONT_PORT:-5173}
      SPRING_PROFILES_ACTIVE: dev
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    volumes:
      - ./backend:/app
      - gradle-cache-backend:/home/gradle/.gradle
      - /app/build
      - /app/.gradle
    depends_on:
      postgres:
        condition: service_healthy
      flying-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:8080/api/health"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5m
    develop:
      watch:
        - action: restart
          path: ./backend/src/main
        - action: restart
          path: ./backend/build.gradle.kts
    networks:
      - app-network
  flying-service:
    build:
      context: ./flying.service
      dockerfile: Dockerfile
    container_name: aterrizar-flying
    restart: unless-stopped
    environment:
      SERVER_PORT: 8081
      CORS_ALLOWED_ORIGIN: http://localhost:${FRONT_PORT:-5173}
      SPRING_PROFILES_ACTIVE: dev
    ports:
      - "${FLYING_SERVICE_PORT:-8081}:8081"
    volumes:
      - ./flying.service:/app
      - gradle-cache-flying:/home/gradle/.gradle
      - /app/build
      - /app/.gradle
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:8081/ping"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5m
    develop:
      watch:
        - action: restart
          path: ./flying.service/src/main
        - action: restart
          path: ./flying.service/build.gradle.kts
    networks:
      - app-network
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: aterrizar-frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:${BACKEND_PORT:-8080}
      VITE_FLYING_SERVICE_URL: http://localhost:${FLYING_SERVICE_PORT:-8081}
    ports:
      - "${FRONT_PORT:-5173}:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
      - /app/dist
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network
volumes:
  pgdata:
    name: aterrizar-pgdata
  gradle-cache-backend:
    name: aterrizar-gradle-backend
  gradle-cache-flying:
    name: aterrizar-gradle-flying
networks:
  app-network:
    name: aterrizar-network
    driver: bridge
